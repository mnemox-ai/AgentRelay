工作目錄 C:/Users/johns/projects/agent-relay。Batch 1 Quick Fixes（Scan Report #3, #10, #12, #13, #25, #26）。修以下問題：(1) src/agentrelay/api/app.py 第 49 行左右 FastAPI version 從 "0.4.0" 改成 "0.6.0"。(2) src/agentrelay/domain/validation_result.py 第 25 行 validated_at 型別改成 datetime | None = None。(3) src/agentrelay/services/quota_service.py 第 33 行 task: object 改成正確的 Task 型別（from agentrelay.models.task import Task）。(4) src/agentrelay/repositories/ledger_repo.py 刪除未使用的 import uuid。(5) src/agentrelay/models/agent.py 新增 AgentStatus enum（active/suspended/banned），status 欄位改用此 enum 的 value。每改一個檔案先 Read 確認行號。全部改完跑 cd C:/Users/johns/projects/agent-relay && python -m pytest tests/ -v 確認全部 pass。git add 修改的檔案 && git commit -m "fix: version mismatch, type annotations, unused import, AgentStatus enum (scan #3,#12,#13,#25,#26)"

工作目錄 C:/Users/johns/projects/agent-relay。Batch 2 Enum + Error Handling（Scan Report #1, #7, #8, #9, #14, #15, #16, #17, #18, #19, #20）。修以下問題：(1) src/agentrelay/config.py 第 9 行 DATABASE_URL default 改成不含密碼的 placeholder，例如 "postgresql+asyncpg://localhost/agentrelay"，讓沒設 env 時會連線失敗而非用 hardcoded creds。(2) src/agentrelay/api/routes/tasks.py 所有 raw string status（"open", "validating" 等）改用 TaskStatus enum（from agentrelay.domain.task_spec import TaskStatus）。(3) src/agentrelay/services/expiration_service.py 第 36, 57 行 hardcoded string 改用 TaskStatus enum。(4) src/agentrelay/security/output_sanitizer.py 第 59 行 bare except 加 logging.warning。(5) src/agentrelay/services/validation_service.py 第 80 行加 ValidatorType enum 反向轉換的 comment 說明（或加 from_value classmethod）。(6) src/agentrelay/mcp_server.py 多處 except Exception 改成分別 catch ValueError（驗證錯誤）和 Exception（DB 錯誤），對 uuid.UUID() 呼叫加 try/except ValueError。(7) src/agentrelay/services/notification_service.py 第 43 行 log level 從 DEBUG 改 WARNING。(8) src/agentrelay/api/routes/tasks.py Redis except Exception: pass 加 logger.warning。(9) src/agentrelay/services/task_service.py 第 105-116 行 except ValueError: pass 加 logger.warning。(10) src/agentrelay/api/routes/ws.py 第 15-20 行加 except Exception catch（timeout, protocol error）加 logger.warning。每改一個檔案先 Read 確認行號和 context。全部改完跑 cd C:/Users/johns/projects/agent-relay && python -m pytest tests/ -v 確認全部 pass。git add -A && git commit -m "fix: hardcoded creds, enum consistency, error handling improvements (scan #1,#7,#8,#9,#14-#20)"

工作目錄 C:/Users/johns/projects/agent-relay。Batch 3 Layer Violations（Scan Report #2, #4, #5, #6）。修以下問題：(1) dashboard.py（#2）：建立 src/agentrelay/services/dashboard_service.py，把 dashboard.py 裡的 ~200 行 raw SQLAlchemy 查詢搬進去。建立 src/agentrelay/repositories/dashboard_repo.py 處理 DB 查詢。dashboard route 只呼叫 DashboardService。(2) tasks.py routes（#4）：移除直接 instantiate TaskRepository/SubmissionRepository/AgentRepository/LedgerRepository/ReputationRepository 的程式碼，改用現有的 TaskService 或建立新 method。(3) agents.py routes（#5）：建立 src/agentrelay/services/agent_service.py（register_agent, get_agent），routes 改呼叫 AgentService。(4) validation.py routes（#6）：移除直接 select(ValidationRun)，改用 ValidationService 或建立對應方法。每個改動先 Read 原始檔案確認結構。遵守分層規則：API → Service → Repository → Models。全部改完跑 cd C:/Users/johns/projects/agent-relay && python -m pytest tests/ -v 確認全部 pass。git add -A && git commit -m "refactor: fix layer violations in dashboard, tasks, agents, validation routes (scan #2,#4,#5,#6)"

工作目錄 C:/Users/johns/projects/agent-relay。Batch 4 Test Quality（Scan Report #21, #22, #23, #24）。修以下問題：(1) tests/test_websocket.py 第 172 行 async def test_broadcast_no_clients 如果用 sync TestClient 會有問題，檢查並修正 async/sync 一致性。(2) tests/test_mcp_e2e.py 第 370, 382 行 pytest.raises(Exception) 改成具體的 exception type（ValueError 或自定義 exception）。(3) tests/test_mcp_server.py 第 170, 200 行同上。(4) tests/test_websocket.py 第 151 行 assert status_code in (200, 201, 409) 改成 assert 單一明確的 status code。每個改動先 Read 確認 context。全部改完跑 cd C:/Users/johns/projects/agent-relay && python -m pytest tests/ -v 確認全部 pass。git add -A && git commit -m "test: improve test quality — specific exceptions, strict assertions (scan #21-#24)"

工作目錄 C:/Users/johns/projects/agent-relay。Batch 5 更新 SCAN_REPORT.md。Read C:/Users/johns/projects/agent-relay/SCAN_REPORT.md，在每個 finding 後面加上修復狀態（✅ Fixed in batch X）。更新 Summary 表格加上 Fixed 欄位。git add SCAN_REPORT.md && git commit -m "docs: update scan report with fix status" && git push
