工作目錄 C:/Users/johns/projects/agent-relay。Task 44: 建立 MCP Server。建立 src/agentrelay/mcp_server.py，用 FastMCP 3.x 直接 import 現有 services（TaskService, ValidationService, QuotaService）。Tools: list_tasks, get_task, create_task, claim_task, submit_task, get_agent_reputation。api_key 作為 tool 參數。加 src/agentrelay/__main__.py 入口讓 python -m agentrelay.mcp 可啟動。加 tests/test_mcp_server.py 測試所有 MCP tools。pyproject.toml 加 fastmcp>=3.0 依賴。完成後跑 cd C:/Users/johns/projects/agent-relay && python -m pytest tests/ -v 確認全部 pass。在 ROADMAP.md 標記 Task 44 為 ✅。git add 相關檔案 && git commit -m "feat: add MCP server exposing task CRUD + claim + submit tools"

工作目錄 C:/Users/johns/projects/agent-relay。Task 45: 建立 SKILL.md template。建立 .skills/agentrelay/SKILL.md，包含：AgentRelay 簡介（一段）、連接方式（MCP stdio 指令）、可用 MCP tools 列表加參數說明、完整 workflow 範例（register → list → claim → execute → submit）、錯誤處理指南。風格：開發者語調，不要行銷語調。完成後跑 cd C:/Users/johns/projects/agent-relay && python -m pytest tests/ -v 確認沒壞。在 ROADMAP.md 標記 Task 45 為 ✅。git add .skills/ ROADMAP.md && git commit -m "docs: add SKILL.md template for universal agent onboarding"

工作目錄 C:/Users/johns/projects/agent-relay。Task 46: Agent auto-discovery via MCP protocol。在 src/agentrelay/mcp_server.py 加 discover_capabilities tool：回傳系統支援的 TaskType、TaskDifficulty、validation_method 清單，回傳 open tasks 數量統計（by type/difficulty），回傳系統版本和 API 狀態。加 MCP resource agentrelay://status（server info + task stats）。加 tests 驗證 discover_capabilities 回傳正確資料。完成後跑 cd C:/Users/johns/projects/agent-relay && python -m pytest tests/ -v 確認全部 pass。在 ROADMAP.md 標記 Task 46 為 ✅。git add -A && git commit -m "feat: add agent auto-discovery via MCP protocol"

工作目錄 C:/Users/johns/projects/agent-relay。Task 47: End-to-end MCP 整合測試。建立 tests/test_mcp_e2e.py，模擬完整 MCP 流程：1. discover_capabilities 查詢系統能力 2. create_task 建立測試任務 3. list_tasks 查看可用任務 4. claim_task 認領 5. submit_task 提交結果 6. 驗證任務狀態變為 COMPLETED 7. get_agent_reputation 確認 reputation 更新。用 pytest-asyncio 跑。完成後跑 cd C:/Users/johns/projects/agent-relay && python -m pytest tests/ -v 確認全部 pass。在 ROADMAP.md 標記 Task 47 為 ✅，Phase 5 標記為 ✅。更新 pyproject.toml version 為 0.6.0。更新 CLAUDE.md 加上 Phase 5 相關檔案位置。git add -A && git commit -m "feat: complete Phase 5 MCP integration with E2E tests"

工作目錄 C:/Users/johns/projects/agent-relay。Task 48: Full codebase scan。讀取整個 src/agentrelay/ 和 tests/ 目錄的所有檔案，檢查：1. import 錯誤或 circular imports 2. 型別不一致（enum vs string, UUID vs str）3. 未使用的 import 或變數 4. 錯誤處理遺漏（bare except, 沒 catch 的 async error）5. 安全問題（hardcoded secrets, SQL injection, 未 sanitize 的輸入）6. 分層違規（API 直接碰 Repository, Service 直接碰 ORM session）7. 版本號不一致（pyproject.toml vs app.py vs README）8. 測試覆蓋缺口（有 service 但沒對應 test）9. TODO/FIXME/HACK 標記 10. Docker/CI 配置過時。不要修改任何檔案！只用 Read/Grep/Glob 工具掃描。把所有問題寫入 C:/Users/johns/projects/agent-relay/SCAN_REPORT.md，格式用 markdown 表格：序號、嚴重度（Critical/High/Medium/Low）、檔案路徑、行號、問題描述。git add SCAN_REPORT.md && git commit -m "chore: add full codebase scan report for Phase 0-5"
